{
  "name": "parallaxin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run --prefix site dev",
    "build": "npm run --prefix site build",
    "preview": "npm run --prefix site preview",
    "check": "npm run --prefix site check",
    "lint:links": "npm run --prefix site lint:links"
  },
  "dependencies": {
    "astro": "^5.18.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}